@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
if(@ENABLE_CUDA@)
    find_dependency(CUDAToolkit REQUIRED)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/pbe_cudaTargets.cmake")

check_required_components(pbe_cuda)
