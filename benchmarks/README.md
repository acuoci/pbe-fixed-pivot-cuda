# Benchmarks

Optional performance benchmark targets should live in this directory. Keep them
separate from correctness tests, analytical verification cases, and worked
examples so benchmark dependencies and runtime cost do not affect normal builds.
