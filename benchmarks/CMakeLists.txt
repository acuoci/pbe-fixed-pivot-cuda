# =============================================================================
# benchmarks/CMakeLists.txt
#
# Optional performance benchmarks live here. No benchmark executables are
# registered yet; the directory exists so future benchmark targets stay separate
# from reusable library code, verification cases, and examples.
# =============================================================================

message(STATUS "pbe_cuda: no benchmark targets are currently defined")
